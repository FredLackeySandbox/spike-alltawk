Brain Dump (20260722 064250)

-----

So we're gonna be building this application in React JavaScript, with a Node.js backend. We'll be building it for a DynamoDB database.

We'll be building in Docker with a Fastify or a Hono API frontend for the Docker domain database Node.js packages that will be in a Hono or Fastify API shell, with eventually being a Lambda wrapper or harness.

But yeah, React JavaScript, DynamoDB, hosted in Docker for local development.
